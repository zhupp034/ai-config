interface:
  display_name: "通用项目初始化"
  short_description: "补齐单仓 AI 协作与交付文档基线，仅创建缺失文件"
  default_prompt: "Use $project-ai-init to inspect a single repository, detect missing AI collaboration baseline docs, and create only the missing artifacts with project-real placeholders."
